# E-Commerce Next.js Project

Современный интернет-магазин на Next.js 14 с TypeScript и Tailwind CSS.

## 🚀 Особенности

- ✅ Next.js 14 (App Router)
- ✅ TypeScript
- ✅ Tailwind CSS
- ✅ Framer Motion (анимации)
- ✅ Полностью адаптивный дизайн
- ✅ Страница каталога с фильтрами
- ✅ Детальная страница продукта
- ✅ Loading и Error страницы
- ✅ 404 страница

## 📦 Установка

```bash
# Установите зависимости
npm install

# Запустите dev сервер
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📁 Структура проекта

```
ecommerce-nextjs/
├── app/
│   ├── products/
│   │   ├── [id]/
│   │   │   └── page.tsx      # Страница детального просмотра
│   │   └── page.tsx          # Каталог продуктов
│   ├── globals.css           # Глобальные стили
│   ├── layout.tsx            # Основной layout
│   ├── page.tsx              # Главная страница
│   ├── loading.tsx           # Страница загрузки
│   ├── error.tsx             # Страница ошибки
│   └── not-found.tsx         # 404 страница
├── components/
│   ├── layout/
│   │   ├── Header.tsx        # Хедер
│   │   └── Footer.tsx        # Футер
│   └── ui/
│       └── ProductCard.tsx   # Карточка продукта
├── data/
│   └── products.ts           # Моковые данные продуктов
└── types/
    └── index.ts              # TypeScript типы
```

## 🎨 Как изменить дизайн

### Цвета

Откройте `tailwind.config.js`:

```js
theme: {
  extend: {
    colors: {
      primary: '#000000',    // Основной цвет
      secondary: '#ffffff',  // Второстепенный
      accent: '#f5f5f5',     // Акцентный
      muted: '#666666',      // Приглушенный
    },
  },
}
```

### Шрифты

В `tailwind.config.js`:

```js
fontFamily: {
  sans: ['Helvetica Neue', 'Arial', 'sans-serif'],
}
```

## 📝 Добавление новых продуктов

Откройте `data/products.ts` и добавьте новый объект:

```typescript
{
  id: 7,
  name: 'New Product',
  category: 'Tops & T-Shirts',
  price: 99,
  status: 'New',
  images: ['https://...'],
  colors: [
    { name: 'Black', code: '#000000', image: 'https://...' }
  ],
  sizes: ['S', 'M', 'L', 'XL'],
  description: 'Product description',
  reviews: [],
}
```

## 🛠️ Технологии

- **Next.js 14** - React фреймворк
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **Framer Motion** - Анимации
- **Lucide React** - Иконки

## 📱 Адаптивность

Проект полностью адаптивен и работает на всех устройствах:
- 📱 Mobile (320px+)
- 📱 Tablet (768px+)
- 💻 Desktop (1024px+)
- 🖥️ Large Desktop (1280px+)

## 🔧 Команды

```bash
npm run dev      # Запуск development сервера
npm run build    # Сборка для production
npm run start    # Запуск production сервера
npm run lint     # Проверка кода
```

## 📄 Лицензия

MIT
